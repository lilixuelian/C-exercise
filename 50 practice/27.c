#include<stdio.h>

int main (void){
	
	scanf("%lf %d", sum, m);
	
	for(i = 0; i < m; i++){
		scanf("%d", n);
		for(j = 0; j < n; j++){
			n = getchar();
			scanf("%f", &count)
			printf("%s:%f", n, count);
		}
	}
	
	return 0;
}
