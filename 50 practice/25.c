#include<stdio.h>
#include<math.h>

int main (void){
	
	int n;
	double r1, r2, p1, p2;
	
	scanf("%f %f %f %f", &r1, &p1, &r2, &p2);
	
	a1 = r1 * cos(p1);
	a2 = r2 * cos(p2);
	b1 = r1 * 
	
	return 0;
}
