#include<stdio.h>
int sum_n(int a[  ]){
	int j;
	int sum = 0;
	
	if(a[n])
	if(a[n] % 2 == 0){
		sum += 0.1 / a[n - 1];
	}
	else
	    sum -= 0.1 / a[n - 1];
}

int main (void){
	
	int m, i;
	int a[100]; 
	
	scanf("%d", &m);
	
	for(i = 0; i < m; i++){
		scanf("%d", &a[i]); 
	}
	
	
	
	return 0;
}
